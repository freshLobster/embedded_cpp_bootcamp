# Troubleshooting - 10_sim ex03_ci_harness

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a CI manifest generator that lists produced artifacts.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
