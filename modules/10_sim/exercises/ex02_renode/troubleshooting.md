# Troubleshooting - 10_sim ex02_renode

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a parser that counts peripheral lines in Renode logs.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
