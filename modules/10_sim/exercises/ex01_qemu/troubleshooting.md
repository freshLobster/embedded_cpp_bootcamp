# Troubleshooting - 10_sim ex01_qemu

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a parser that detects successful boot markers in QEMU logs.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
