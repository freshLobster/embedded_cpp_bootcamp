# Troubleshooting - 12_cuda_perf ex03_jetson

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a helper that reports likely bottlenecks for Jetson-class systems.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
