# Troubleshooting - 12_cuda_perf ex02_divergence

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a divergence metric computation from warp masks.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
