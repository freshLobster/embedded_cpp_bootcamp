#pragma once

// Exercise entry point for the learner build.
// You will implement the buffer and then make exercise() run a self-check
// that returns 0 on success and a small, non-zero code on failure.
// Keeping the return codes stable helps you triage failures quickly.
int exercise();
