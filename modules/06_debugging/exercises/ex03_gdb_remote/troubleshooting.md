# Troubleshooting - 06_debugging ex03_gdb_remote

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a checksum routine with a predictable call chain for debugging practice.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
