# Troubleshooting - 08_cross_deploy ex01_cross_build

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a function that reports the compile-time target architecture.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
