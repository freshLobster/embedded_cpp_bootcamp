# Troubleshooting - 04_architecture ex02_tracing

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a lightweight tracer that records span durations with RAII.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
