# Troubleshooting - 04_architecture ex01_topic_contract

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a topic-contract parser for name/type/rate strings.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
