# Troubleshooting - 11_cuda_basics ex01_env

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a CUDA environment detector based on compile-time flags.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
