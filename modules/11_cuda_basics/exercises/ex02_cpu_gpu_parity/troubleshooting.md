# Troubleshooting - 11_cuda_basics ex02_cpu_gpu_parity

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a CPU reference vector add used for parity checks.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
