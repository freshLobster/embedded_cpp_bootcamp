# Troubleshooting - 07_perf ex01_perf_baseline

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a dot-product routine to establish a baseline workload.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
