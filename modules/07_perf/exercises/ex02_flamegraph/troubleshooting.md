# Troubleshooting - 07_perf ex02_flamegraph

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a multi-level call chain to generate a meaningful stack profile.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
