# Troubleshooting - 07_perf ex03_perf_regression

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a string-join routine that avoids unnecessary reallocations.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
