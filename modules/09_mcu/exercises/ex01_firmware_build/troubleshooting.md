# Troubleshooting - 09_mcu ex01_firmware_build

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a UART-friendly encoding and decoding for sensor samples.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
