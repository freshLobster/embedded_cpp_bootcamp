# Troubleshooting - 09_mcu ex03_uart_bridge

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a UART frame encoder/decoder with a simple checksum.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
