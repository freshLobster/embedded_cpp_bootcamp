# Troubleshooting - 05_scheduling ex03_priority

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a priority task queue that runs higher-priority work first.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
