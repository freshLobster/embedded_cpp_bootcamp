# Troubleshooting - 05_scheduling ex02_timeout_fallback

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a timeout wrapper that returns a fallback value when work exceeds a deadline.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
