# Troubleshooting - 05_scheduling ex01_latency_budget

1) **Build fails due to TODO placeholder**
   - Fix: remove the `#error` line after you implement a latency statistics calculator that computes p50 and p95.

2) **Tests fail with assertion**
   - Fix: inspect `exercise()` and ensure it enforces the required behavior.
